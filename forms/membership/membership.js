/* 
var members = ["Mary","Sam","Joey","Paul","Mike","Char","Jenny","Kennedy","Kaden","Kurt","Anne"]

btSubmit.onclick=function(){
  name = inptName.value
  if (name  === "Mary" || name  === "Sam" || name  === "Joey" || name  === "Paul" || name  === "Mike" || name  === "Char" || name  === "Jenny" || name  === "Kennedy" || name  === "Kaden" || name  === "Kurt" || name  === "Anne") {
    lblSubmit.value = `Welcome back, ${name}!`
  } else  { 
    lblSubmit.value = "You're being added as a member." 
    members.push(name)
    } 
}

console.log(members)
*/ 